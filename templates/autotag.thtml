{# begin {templatelocation} #}
{!if source_url}<a href="{source_url}" target="_blank">{!endif}
<span class="atag quote">{quote}</span> <span class="atag quoteby">({quoted})</span>
{!if source_url}</a>{!endif}
{# end {templatelocation} #}
